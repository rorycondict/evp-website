[project]
name = "evp-backend"
version = "1.0.0"
description = "Backend for Edinburgh VenturePoint's website."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pydantic-settings>=2.15.0",
    "resend>=2.43.0",
    "ruff>=0.16.6",
]
